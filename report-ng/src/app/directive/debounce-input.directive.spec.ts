import { DebounceInputDirective } from './debounce-input.directive';

describe('DebounceInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceInputDirective();
    expect(directive).toBeTruthy();
  });
});
